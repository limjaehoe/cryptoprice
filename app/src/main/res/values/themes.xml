<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Cryptoprice" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/red</item>

        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Cryptoprice" parent="Base.Theme.Cryptoprice" />

    <style name="Base.Theme.Cryptoprice.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/red</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/graph</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Cryptoprice</item>
    </style>
</resources>